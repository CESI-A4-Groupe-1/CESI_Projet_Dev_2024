export default class Restaurant {
    constructor(id, id_rest, name, address, id_tag, descrition) {
        this.id = id,
        this.id_rest = id_rest,
        this.name = name,
        this.address = address,
        this.id_tag = id_tag,
        this.descrition = descrition
    }
}